{ An interface type in a unit's interface section cannot be exported: the
  method table it needs does not travel in the object, so an importer would
  get an interface with no methods. Refused at the unit, not at the import.
  Made by a machine. PUBLIC DOMAIN (CC0-1.0) }
program l006;

uses IU;

begin
end.
