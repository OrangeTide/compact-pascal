{ uses Files resolves to the system unit first, so a unit of that name could
  never be imported by anybody.
  Made by a machine. PUBLIC DOMAIN (CC0-1.0) }
unit Files;

interface

procedure P;

implementation

procedure P;
begin
end;

end.
