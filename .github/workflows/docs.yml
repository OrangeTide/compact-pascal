name: Build and Deploy Docs

on:
  push:
    branches: [main]
    paths:
      - 'PLAN.md'
      - 'doc/**'
      - 'pages/**'
      - '.github/workflows/docs.yml'
      - 'Makefile'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Pandoc and fonts
        run: sudo apt-get update && sudo apt-get install -y pandoc fonts-texgyre

      - name: Install Typst
        run: |
          curl -fsSL https://github.com/typst/typst/releases/latest/download/typst-x86_64-unknown-linux-musl.tar.xz \
            | tar -xJ --strip-components=1 -C /usr/local/bin/

      - name: Build PDFs
        run: make pdf

      - name: Assemble site
        run: |
          mkdir -p _site
          cp pages/index.html _site/
          cp build/compact-pascal-wp.pdf _site/
          cp build/compact-pascal-ref.pdf _site/
          cp doc/compact-pascal-wp.md _site/
          cp doc/compact-pascal-ref.md _site/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
